out/
worktrees/
*.log
node_modules/
.DS_Store
# Exclude backup / draft requirement docs from repo
需求bak/
# Keep dummy_repo contents but not its .git (avoid embedded repo)
examples/dummy_repo/.git/
.cursor/

# local/backup
*.bak
*.tmp
*.swp
*.swo
.DS_Store

# docs build / exports (if any)
docs/**/bak/
docs/
